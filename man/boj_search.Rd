% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{boj_search}
\alias{boj_search}
\title{Search Bank of Japan series metadata}
\usage{
boj_search(
  pattern,
  db = NULL,
  fields = c("series_code", "name", "category", "notes"),
  regex = FALSE,
  ignore_case = TRUE,
  metadata = NULL,
  lang = boj_default("lang", "en"),
  cache = TRUE,
  refresh = FALSE,
  max_age = 24 * 60 * 60,
  cache_dir = boj_default("cache_dir", tools::R_user_dir("bojapi", "cache")),
  include_groups = FALSE,
  wait = boj_default("wait", 1),
  timeout = boj_default("timeout", 30),
  retries = boj_default("retries", 3)
)
}
\arguments{
\item{pattern}{One non-empty search string or regular expression.}

\item{db}{One or more database identifiers. Not needed when \code{metadata} is
supplied.}

\item{fields}{Metadata columns to search. Defaults to series code, name,
category, and notes.}

\item{regex}{Treat \code{pattern} as a regular expression. The default performs a
literal substring search.}

\item{ignore_case}{Ignore letter case.}

\item{metadata}{Optional data frame returned by \code{\link[=boj_metadata]{boj_metadata()}} or
\code{\link[=boj_cache]{boj_cache()}}.}

\item{lang}{Response language, \code{"en"} or \code{"jp"}. Japanese responses also
contain some English fields, but this function consistently returns the
requested language in \code{name}, \code{unit}, \code{category}, and \code{notes}.}

\item{cache}{Whether to read and write the on-disk metadata cache.}

\item{refresh}{If \code{TRUE}, ignore an existing cache entry.}

\item{max_age}{Maximum cache age in seconds; default 86,400 (24 hours).}

\item{cache_dir}{Metadata cache directory.}

\item{include_groups}{Include matching hierarchy-heading rows.}

\item{wait}{Seconds between automatic requests. The default is one second to
avoid high-frequency access prohibited by the BOJ.}

\item{timeout}{Request timeout in seconds.}

\item{retries}{Number of retries for network and transient server errors.}
}
\value{
A filtered metadata tibble.
}
\description{
Searches current BOJ metadata in the same spirit as \code{WDI::WDIsearch()}.
Supply a database to fetch/cache metadata, or pass a metadata tibble directly
for an offline or reproducible search.
}
\examples{
\dontrun{
boj_search("U.S. Dollar", db = "FM08")
boj_search("FXERM07", db = "FM08", lang = "jp")
boj_search("exchange|euro", db = "FM08", regex = TRUE)
}
}
