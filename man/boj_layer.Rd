% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{boj_layer}
\alias{boj_layer}
\title{Retrieve Bank of Japan time-series data by hierarchy}
\usage{
boj_layer(
  db,
  frequency,
  layer,
  start_date = NULL,
  end_date = NULL,
  lang = boj_default("lang", "en"),
  wide = FALSE,
  wait = boj_default("wait", 1),
  timeout = boj_default("timeout", 30),
  retries = boj_default("retries", 3)
)
}
\arguments{
\item{db}{BOJ database identifier.}

\item{frequency}{One of \code{"CY"}, \code{"FY"}, \code{"CH"}, \code{"FH"}, \code{"Q"}, \code{"M"},
\code{"W"}, or \code{"D"}.}

\item{layer}{One to five hierarchy values supplied as a vector or a
comma-separated string. \code{"*"} is a wildcard; for example, \code{c(1, "*", 2)}.}

\item{start_date, end_date}{Optional BOJ period codes. Accepted request forms
are \code{YYYY} or \code{YYYYPP}; daily and weekly filters also use \code{YYYYMM}.}

\item{lang}{Response language, \code{"en"} or \code{"jp"}.}

\item{wide}{If \code{FALSE} (default), return normalized long data. If \code{TRUE},
return \code{time}, parsed \code{date}, and one column per code or alias.}

\item{wait}{Requested seconds between automatic requests. The default is one
second, and values below one are treated as one whenever another request is
needed, to avoid high-frequency access prohibited by the BOJ.}

\item{timeout}{Request timeout in seconds.}

\item{retries}{Number of retries for network and transient server errors.}
}
\value{
A long or wide tibble with the same structure as \code{\link[=boj_data]{boj_data()}}.
}
\description{
Uses the BOJ layer API to retrieve all series matching a database hierarchy
and frequency. The function follows \code{NEXTPOSITION} automatically. A layer
condition that identifies more than 1,250 series is rejected by the BOJ API;
this count is evaluated before the frequency filter.
}
\examples{
\dontrun{
boj_layer(
  db = "BP01", frequency = "M", layer = c(1, 1, 1),
  start_date = "202504", end_date = "202509"
)
}
}
