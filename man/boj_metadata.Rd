% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{boj_metadata}
\alias{boj_metadata}
\title{Retrieve Bank of Japan series metadata}
\usage{
boj_metadata(
  db,
  lang = boj_default("lang", "en"),
  cache = TRUE,
  refresh = FALSE,
  max_age = 24 * 60 * 60,
  cache_dir = boj_default("cache_dir", tools::R_user_dir("bojapi", "cache")),
  include_groups = TRUE,
  timeout = boj_default("timeout", 30),
  retries = boj_default("retries", 3),
  wait = boj_default("wait", 1)
)
}
\arguments{
\item{db}{BOJ database identifier, such as \code{"FM08"}.}

\item{lang}{Response language, \code{"en"} or \code{"jp"}. Japanese responses also
contain some English fields, but this function consistently returns the
requested language in \code{name}, \code{unit}, \code{category}, and \code{notes}.}

\item{cache}{Whether to read and write the on-disk metadata cache.}

\item{refresh}{If \code{TRUE}, ignore an existing cache entry.}

\item{max_age}{Maximum cache age in seconds; default 86,400 (24 hours).}

\item{cache_dir}{Metadata cache directory.}

\item{include_groups}{Include hierarchy-heading rows whose \code{series_code} is
blank. These rows are useful when constructing \code{\link[=boj_layer]{boj_layer()}} queries.}

\item{timeout}{Request timeout in seconds.}

\item{retries}{Number of retries for network and transient server errors.}

\item{wait}{Seconds between automatic requests. The default is one second to
avoid high-frequency access prohibited by the BOJ.}
}
\value{
A tibble with normalized metadata. \code{start_time} and \code{end_time}
preserve exact BOJ period codes. Parsed date columns indicate the first day
of each period and are \code{NA} for hierarchy headings.
}
\description{
Downloads the complete metadata table for one BOJ database. Metadata
includes both hierarchy headings (blank \code{series_code}) and actual series.
By default it is cached for 24 hours, matching the API's daily metadata
update cycle and reducing load on the BOJ service.
}
\examples{
\dontrun{
metadata <- boj_metadata("FM08", lang = "jp")
subset(metadata, is_series)
}
}
