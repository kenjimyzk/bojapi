% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{boj_data}
\alias{boj_data}
\title{Retrieve Bank of Japan time-series data by series code}
\usage{
boj_data(
  db,
  code,
  start_date = NULL,
  end_date = NULL,
  lang = boj_default("lang", "en"),
  wide = FALSE,
  wait = boj_default("wait", 1),
  timeout = boj_default("timeout", 30),
  retries = boj_default("retries", 3)
)
}
\arguments{
\item{db}{BOJ database identifier, such as \code{"FM08"}.}

\item{code}{Character vector of series codes without the database prefix.
For example, use \code{"MADR1Z@D"}, not \code{"IR01'MADR1Z@D"}. Names become
aliases in the \code{series} column and wide output.}

\item{start_date, end_date}{Optional BOJ period codes. Accepted request forms
are \code{YYYY} or \code{YYYYPP}; daily and weekly filters also use \code{YYYYMM}.}

\item{lang}{Response language, \code{"en"} or \code{"jp"}.}

\item{wide}{If \code{FALSE} (default), return normalized long data. If \code{TRUE},
return \code{time}, parsed \code{date}, and one column per code or alias.}

\item{wait}{Seconds between automatic requests. The default is one second to
avoid high-frequency access prohibited by the BOJ.}

\item{timeout}{Request timeout in seconds.}

\item{retries}{Number of retries for network and transient server errors.}
}
\value{
A tibble. Long output contains \code{db}, \code{series}, \code{series_code}, \code{name},
\code{unit}, \code{frequency}, \code{category}, \code{last_update}, \code{time}, \code{date}, and
\code{value}. \code{time} preserves the exact BOJ period code. \code{date} is the first
day of non-daily periods (April 1 for fiscal years); use \code{time} when exact
period semantics matter.
}
\description{
\code{boj_data()} is the main data function. It accepts one or more BOJ series
codes, splits requests into safe batches of 250 codes, follows
\code{NEXTPOSITION} until complete, and returns one observation per row. All codes
in a BOJ code request must have the same frequency.
}
\details{
A named \code{code} vector creates convenient aliases, following the style of
\code{WDI::WDI()}. The original BOJ code is always retained in \code{series_code}.
}
\examples{
\dontrun{
fx <- boj_data(
  db = "FM08",
  code = c(usd_yen = "FXERM07"),
  start_date = "202401",
  end_date = "202412"
)

boj_data(
  "FM08",
  c(usd_yen = "FXERM07", euro_yen = "FXERM09"),
  start_date = "202401",
  wide = TRUE
)
}
}
